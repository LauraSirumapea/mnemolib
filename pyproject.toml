[project]
name = "mnemolib"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "LauraSirumapea", email = "lauraawise07@gmail.com" }
]
requires-python = ">=3.11"
dependencies = []

[project.scripts]
mnemolib = "mnemolib:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
